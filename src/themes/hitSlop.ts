import themeStyles from "./themeStyles";

const hitSlop = {
  bottom: themeStyles.namedSpacing("xs"),
  left: themeStyles.namedSpacing("xs"),
  right: themeStyles.namedSpacing("xs"),
  top: themeStyles.namedSpacing("xs"),
};

export default hitSlop;
